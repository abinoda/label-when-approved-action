steps:
    - name: Approvals
      uses: pullreminders/label-when-approved-action@ashok
      env:
        APPROVALS: "1"
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ADD_LABEL: "approved"
